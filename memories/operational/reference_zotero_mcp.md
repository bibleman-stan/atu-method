---
name: zotero-mcp-integration
description: "Zotero 9 + MCP setup so Claude can read/write Stan's Zotero library; config, write-scopes, safety model, division of labor, pending test"
metadata: 
  node_type: memory
  type: reference
  originSessionId: 2af06a3d-3c5c-4bb8-af2d-f02d2b7d39d1
---

> **PROVENANCE**: recovered 2026-08-06 from Claude Code file-history (`18327914-8fd6-4400-a057-2a38aaf1a09f/ef3c38630deecd3e@v16`); state as of 2026-05-26 (snapshot mtime); possibly stale — re-verify before relying.

Stan upgraded **Zotero 6 → 9.0.3** (2026-05-22) and wired in an MCP so Claude can help clean up his library (smart title-case, fixing messy imports, restructuring book reviews to SBL, etc.).

**Backup before upgrade:** full data dir copied (verified byte-for-byte) to `C:\Users\bibleman\Zotero-backup-2026-05-22`. Rollback = quit Zotero, copy it back over `C:\Users\bibleman\Zotero`. Data dir = `C:\Users\bibleman\Zotero`.

**Post-upgrade workflow state (all good):** BBT updated to its Z9 build + re-enabled; citation keys intact and correct (`author-yyyy`, e.g. `Browning-2009`, now in Zotero's native Citation Key field). Obsolete plugins removed (Night for Zotero — Z9 has native dark mode). ZotFile is dead on Z7+ (don't reinstall; native File Renaming + native annotation extraction replaced it). Zutilo still enabled.

**MCP server:** plugin = **lricher7329/zotero-mcp-claude-code v1.8.4** (fork of cookjohn; Z7/8/9; fully local — runs its own server inside Zotero, no external API key, no cloud). Server at **`http://127.0.0.1:23120/mcp`**, loopback-only (remote access OFF), **auth-token NOT required** (so no token in the config). **Zotero must be running** for the server to be up.

**Claude Code wiring:** `claude` CLI not on PATH and Stan uses the VS Code extension, so wired via **`C:\Users\bibleman\.mcp.json`** (project-scoped): `{"mcpServers":{"zotero-mcp":{"type":"http","url":"http://127.0.0.1:23120/mcp"}}}`. On Claude Code restart it loads (may prompt to approve the project MCP server — approve "zotero-mcp"). The plugin also has a Client Configuration Generator (Settings → Zotero MCP) but its "Claude Code" output uses a non-standard `serverUrl` key — use `type:http`+`url` instead.

**Write-scope safety model (staged trust):** in Settings → Zotero MCP → Write Scopes, only **"Item metadata"** (create items, edit fields) is ON. The three destructive scopes — **Delete, Bulk operations, Import-from-URL** — are OFF and stay off until Claude has a proven track record. Plan: enable a dangerous scope only transiently *for a specific vetted task*, then turn it back off. Title-casing many items works under plain `metadata` (one-by-one); `Bulk` only needed for true batch ops.

**Division of labor (decided):**
- **Deterministic / every-item → Better BibTeX + Zotero native** (citekeys, attachment renaming, mechanical formatting). Do NOT route these through Claude — BBT does them free/instant.
- **Judgment / messy → Claude via MCP**: smart title-case (NT/OT/proper nouns/non-English that mechanical title-case botches), fixing garbled/incomplete imports, restructuring book reviews to SBL form, semantic tagging, dedup judgment, flagging stale editions.

**VERIFIED LIVE 2026-05-22:** restart loaded the tools (deferred `mcp__zotero-mcp__*`, load via ToolSearch). Read+write round-trip confirmed — searched the Sweeney review (`MI58WANP`); append-`[MCP-TEST]`-then-revert title test on `XRS5HPTJ` succeeded; library left pristine. Tools are deferred each session → ToolSearch `select:mcp__zotero-mcp__search_library,...update_item,...get_item_details` etc. before calling. **Zotero must be running** for the server.

**Reviewed-book detail found:** the book Sweeney reviewed is in the library as `XRS5HPTJ` — *Studies in Scriptural Unit Division*, ed. Marjo C. A. Korpel (2002) (Pericope series). So the SBL review form = "Marvin A. Sweeney, review of *Studies in Scriptural Unit Division*, ed. Marjo C. A. Korpel [+ Josef M. Oesch?], *Journal of Semitic Studies* 50.1 (2005): 207–9."

**(a) DONE — Sweeney review (`MI58WANP`) restructured:** `reviewed-title`/`reviewed-author` (Korpel) added to Extra + Related-linked to `XRS5HPTJ` + original title kept. **Review-handling pattern learned (reuse for future reviews):** capture reviewed-title/reviewed-author in Extra; Related-link to the reviewed book; **KEEP a title** (SBL drops the "review of…" clause if the title is empty; Chicago prefers it cleared — no single config is perfect in both). Both styles render the review-of form but with quirks: SBL shows mild redundancy (book title twice); both render "by" not "ed." for an editor → reviews get a ~5-sec hand-finish at cite time. SBL style id = `society-of-biblical-literature-fullnote-bibliography` (NOT bare `society-of-biblical-literature`); generate_bibliography works with installed styles only.

**SBL IS THE DEFAULT STYLE FOR EVERYTHING (Stan, 2026-05-22):** Stan plans to be 98%+ SBL across all coursework + research. Every citation/bibliography/review-handling solution defaults to SBL (`society-of-biblical-literature-fullnote-bibliography`) unless he explicitly says otherwise. Don't ask which style; assume SBL. Citation handling is "very finicky" everywhere — expect quirks and hand-finishing in any style.

**Zotero 9 live citation preview (replaces Zotero 6 toggle):** item pane → right-click the header → **View As → Bibliography Entry** → set style to SBL. Live per-item preview, right-click→Copy to grab. Separate **Style Preview** window (Settings → Cite / dev preview_pane) renders all installed styles at once. Set the header preview to SBL as the standing default.

**Import workflow (decided 2026-05-22):** Stan saves via the **Zotero Connector**; Claude polishes to SBL via MCP. For JSTOR/Brill chapters the Connector often resolves through **Crossref/DOI** → cleaner metadata than a JSTOR scrape (correct author-vs-editor split, real chapter DOI, canonical publisher URL, no proxy junk) **but never carries the PDF** (DOI route is metadata-only). Get the PDF via Zotero's right-click **"Find Available PDF"** (works for OA+DOI) or download+drag as a *stored* copy. **Turn OFF** Settings → General → "Automatically tag items with keywords and subject headings" (it dumped publisher keyword tags). Standard bookSection SBL-cleanup checklist: de-cap publisher (BRILL→Brill), add full subtitle to bookTitle, add place (Leiden for Brill), add series+number, strip any "CHAPTER N" title prefix, clear redundant URL when a DOI exists. **Place of publication — SBL Dec-2025 rule change (corrected 2026-05-26; supersedes an earlier wrong note that called the missing place a "bug").** NOT a bug: **BOTH modern style editions dropped place of publication for books** — SBLHS removed it Dec 2025 (sblhs2.com/2025/12/05/publisher-locations/) AND **CMOS 18 (2024) dropped it too** (verified 2026-05-26: `chicago-notes-bibliography.csl` [18th ed.] has `publisher-place` commented out at line ~4995, prints publisher only). So under CURRENT SBL *and* CURRENT Chicago 18, "Brill, 2003" / "Mohr Siebeck, 2024" with NO place is correct. **Do NOT tell Stan to "add Leiden/Tübingen"** — I did this wrongly twice (first claimed SBL requires it, then claimed Chicago keeps it; both wrong). Only CMOS 17 / older SBL kept place. Open grade-risk: a CMOS-17-minded grader (Musgrave?) may still expect place — check his sample papers.
- Stan's setup: his main Quick Copy setting (`export.quickCopy.setting`) is UNSET, so Ctrl+Shift+C falls back to a default *place-dropping* style — NOT his custom `sbl-2-fullnote-modified-abd.csl`, which actually KEEPS place (place-keeping SBL hybrid). `export.lastStyle` = the modified-abd style; BBT `quickCopyMode` = pandoc.
- **Fix path:** set the citation style explicitly rather than relying on Quick Copy's unset default. Best = Zotero Integration plugin → **Citation Formats** → add a format (Format="Formatted Bibliography", pin the desired CSL) → "Insert Citation (<name>)" command opens the picker and inserts in that pinned style. This is the successor to the retired Citations plugin's insert and the recommended path (no surprise place-drops).
- **Decision (2026-05-26, FINAL): RS6310 uses SBL-2** — Stan briefly tried Chicago, then reverted (biblical-studies program; SBL is his global default). **No course exception — SBL everywhere.** The "Chicago keeps place" rationale that drove the detour was moot (both CMOS18 and SBL-2 drop place; see place note above). Set Quick Copy / the plugin format back to an SBL style. Stock `society-of-biblical-literature-fullnote-bibliography.csl` = current SBL-2 (no place); his custom `sbl-2-fullnote-modified-abd.csl` re-adds place + attempts sigla — pick deliberately.
- **Insert-route lessons (2026-05-26):** the Obsidian Zotero-Integration **"Formatted Bibliography" Citation Format errored** ("CSL style … not found" — it stored the style display-name as the ID; this output is flaky on Zotero 7+). **Reliable route = Zotero Quick Copy (Ctrl+Shift+C → paste).** Set Quick Copy Item Format to the desired SBL style, and **turn ON "Copy as HTML"** so titles paste italicized as markdown (plain-text copy drops italics). Footnotes (Word plugin) = note form WITH parens around (Publisher, Year); the Sources/bibliography list = bibliography form, NO parens — both correct, by design.
- **SBL-2 vs Chicago 18 — for modern sources, nearly identical** (corrected): both dropped place, same skeleton. The ONE real divergence is **abbreviations** — SBL uses sigla (AYBD/ABD/JBL/WUNT from the SBLHS master list), Chicago spells them out. (Place is NOT a differentiator — both editions dropped it; my earlier "place differs" note was wrong.) For book-heavy lists the two are functionally interchangeable; abbreviations only show with journals/series/reference works.
- **Custom style `sbl-2-fullnote-modified-abd.csl` — abbreviation gap (for later, SBL-only):** Stan cobbled it together to get SBL sigla (e.g. AYBD) on reference works, but its `container-title-note` only applies `form="short"` to `article-journal`; the chapter/dictionary branch renders the container-title FULL → that's why AYBD (a bookSection/dictionary entry) never abbreviated. Also vanilla Zotero has no abbreviation field for book-section containers (Journal Abbr exists only on journalArticle), so the siglum has nothing to pull from unless stored manually. Its `publisher` macro DOES keep place (place-keeping SBL hybrid). To make SBL reference-work sigla work: fix the chapter branch to use `form="short"` AND establish where the siglum is stored. Deferred until Stan is next working in SBL.
- Still-real metadata bugs from the ProQuest import (independent of the place question): series number landing in the `volume` field (renders "Vol. 2/603" instead of attaching to series → move to `seriesNumber`; fixed on Marschall B6S4FM83 2026-05-26); German series names stored lowercase then title-cased by CSL ("zum"→"Zum"). Library tags fully stripped 2026-05-22 (was 18, all redundant/import-junk); Stan rebuilding with work-phase tags only.

**ProQuest Ebook Central translator defect (2026-05-26) — Amridge library-proxy book imports.** Distinct from the Connector/DOI chapter path above: whole *books* pulled through the Amridge ProQuest proxy ride in with a consistent defect set — (1) all-caps publisher (`BRILL`→`Brill`), (2) place `City, COUNTRY` in caps (`Boston, UNITED STATES`→`Leiden` for Brill), (3) **editors imported as `author`** (edited volumes mislabeled — fix creatorType author→editor), (4) **series dropped** (re-add, e.g. Pericope N). The reliable library-wide search signature for this class is the place string **`UNITED STATES`** (caps). Fixed the 3 Brill instances 2026-05-26: `USAGSCMT` Laato/de Moor *Theodicy* 2003; `7ZLD5D3F` Korpel/Oesch/Porter *Method in Unit Delimitation* (Pericope 6, 2007); `63JM7M8U` Korpel/de Hoop/Porter *Impact of Unit Delimitation* (Pericope 7, 2008). **URL display norm (decided 2026-05-26):** the SBL CSL (`society-of-biblical-literature-fullnote-bibliography.csl`) prints the `url` field whenever it's non-empty (its only suppression — an `if variable="dimensions"` trick, lines ~1128–1138 — doesn't fire for ordinary books, so URLs leak into book/article citations). Stan's rule: **reserve the `url` field for genuinely web-only items; for print works accessed online, keep the access link as a child "Link to URI" attachment (or in Extra)** — retained + clickable but never rendered by any style. Chose this field-norm over a custom no-URL CSL (lower risk/maintenance). **Sweep rule:** on each cleanup, clear proxy `url`s on print items (ProQuest `…/lib/amridge/…` links are junk — PDF/snapshot attachment already gives access) and relocate any genuinely-useful link to a Link attachment, leaving `url` populated only on web-only types.

Standing arrangement (Stan, 2026-05-26): he will KEEP using the fast ProQuest/Amridge import (speed over clean capture) and rely on **periodic Claude cleanup sweeps** rather than fixing at capture time. So treat this as recurring batch maintenance — when asked to "clean up Zotero," sweep the `UNITED STATES` signature + the publisher/place/editor-type/series defects as a batch. Do NOT suggest he change the import method; the dirty import is a deliberate speed tradeoff. Still-open flags (not fixed): `ULZR7VPQ` Hobbes *Leviathan* POD-reprint place "United States"; `CAQQUP7F` *Delimitation Criticism* dated 2021/Brill but founding ed. is 2000 Assen: Van Gorcum + its attached PDF is mis-named `Korting-2003.pdf`; Korpel name punctuation inconsistent across journalArticles ("Marjo C A" vs "Marjo C. A.").

**(b) DONE — title-case normalization sweep (2026-05-22):** 72 journalArticles audited; 9 fixes applied (5 clear mis-casing, 3 strict-preposition via/beyond/after lowercased, 1 truncated-title completion — Carson). Left German/Greek/transliterated titles untouched.

**File renaming (settled 2026-05-22, painfully):** Zotero native File Renaming template that works in Stan's build = **`{{ citationKey }}`** (matches his BBT citekey scheme exactly; handles author/editor/multi-author/disambiguation). Hard-won gotchas: (1) **colon-prefixed fields render empty** in his build — use `{{ year }}` NOT `{{ :year }}`; the doc's colon syntax fails here. (2) **Conditional control flow (`if`/`else`/`endif`) is ignored** in his build — both branches render, so don't use conditionals. (3) Auto-rename renames the **file on disk, not the display title** — a PDF can be correctly named `Foo-2019.pdf` on disk while the list still shows "PDF"; ALWAYS verify via `get_item_details` → attachment `filename`/`path`, not the displayed title. (4) The right-click **"Rename File from Parent Metadata" option is ABSENT** from his attachment context menu (likely a plugin — Zutilo/Semantic-MCP — suppressing it); don't rely on it. (5) `{{ citationKey }}` comes up empty only for items whose BBT key is unpinned (Extra lacks `Citation Key:`); fix = pin in BBT, but DON'T run library-wide "Rename Files" (his files are already correctly named; batch would break unpinned ones to `_.pdf`). Lesson: check the actual on-disk filename via MCP BEFORE debugging a "rename isn't working" report.

**SBL reference-work sigla in Zotero — SOLVED (2026-05-26, CSL-verified; source: J. David Stark / jdavidstark.com, a contributor to the SBL CSL).** Stan's long-standing pain (ABD/TDNT/TDOT etc. rendering full instead of as sigla) is fixable with STOCK SBL — no custom style needed (the custom `sbl-2-fullnote-modified-abd` is NOT required for this; retire it):
- **Dictionaries/lexicons = the item TYPE, not the style.** Stock SBL has a `signed-dictionary-bib` macro for `entry-dictionary`/`entry-encyclopedia` items = terse `Author. "Title." SIGLUM vol:pages.` (no editor/publisher/date/"Pages…in"). Items typed as **Book Section** route to the generic chapter branch → full apparatus (wrong). **Fix: change item type to "Dictionary Entry"** and put the **siglum** in the **Dictionary Title** field (e.g. `ABD`, `TDNT`, `BDAG`). Stan's Logos-exported dictionary entries (ABD/Freedman 1992, TDOT, TDNT/Kittel, Lexham Bible Dictionary 2016, DPL 2023…) are currently bookSection → re-type them. NB siglum for Freedman's 1992 dict is **ABD** (not "AYBD" — the Logos/Yale rebrand); verify each siglum against SBLHS §8.4. (Tested on UCH4YCFY Wright "Jesus (Person)", bookTitle set to "ABD" 2026-05-26; pending Stan's item-type flip + render confirm.) Item-type change must be done manually in Zotero (MCP update_item can't change itemType). **DEFERRED (2026-05-26)** behind Stan's current RS6310 assignment. **Pre-check CONFIRMED 2026-05-26: Dictionary Entry type works correctly in Zotero 9** (the ZT6 defect Stan remembered is gone). Tested on Witherington "Birth of Jesus": Dictionary Entry renders the **terse** form `Author. "Entry." `*Title*`, pages.` (drops editor/publisher/date); Book Section renders the **full** form (`…Pages X in `*Title*`. Edited by … Publisher, Year.`). **CORRECTED understanding (don't repeat the earlier error of implying terse=better):** the two forms are for DIFFERENT slots, not better/worse. **Bibliography** entry → the **FULL** form (Book Section output: "Pages X in *Title*. Edited by… Publisher, Year") is the SBL-correct one (matches SBLHS §6.3.6 model) → **leave bibliography-cited dictionary articles as Book Section; do NOT convert them.** **Footnote/short reference** → the **siglum** form ("Witherington, *DJG*, 60–74") → that's what Dictionary Entry + siglum-in-title produces, and that's what Stan's ORIGINAL gripe was about (siglum not showing in subsequent *footnote* refs). **RESOLUTION (2026-05-26, Stan-confirmed): KEEP dictionary articles as Book Section. The ~48-item reclassification is CANCELLED.** Stan wants the fuller bibliography entry (source dictionary + editors + publisher + pages) — which is the Book Section full form and is SBL-correct for a bibliography. Converting to Dictionary Entry would STRIP the editors/publisher (wrong for a bibliography). So do NOT convert. The terse/siglum form (e.g. "DJG 60–74") belongs only in FOOTNOTES — a separate, smaller, deferred need for when Stan writes a paper citing reference works in notes (sort out then; verify SBLHS §6.3.6). **Only remaining dictionary touch-up:** **name-suffix jumbles** from Logos import (e.g., "Witherington, **III, Ben**" → "Witherington, Ben, III") — a few items, fix per-item; type-independent. Audit map of the ~48 dictionary entries (ABD/LBD/DJG/ISBE/DOTP/TDOT/TDNT/TWOT/DPL/DLNT/ZEB/WBE + grouped item keys) was compiled 2026-05-26; sigla pending Stan's §8.4 confirmation. Test item UCH4YCFY (Wright "Jesus (Person)") already has bookTitle="ABD".
- **Series sigla (WUNT, WBC):** keep full name in **Series**, add `collection-title-short: WUNT` in **Extra** (stock SBL uses collection-title form="short"). Fixes Marschall WUNT 2/603 etc.
- **Lexica s.v. (BDAG):** stock SBL supports the `Annote: [siglum]` Extra line + `sub verbo` locator.
- **Journals (JBL/NTS):** SBL siglum in **Journal Abbr**; uncheck "Automatically abbreviate journal titles" (Word-plugin Document Pref).
- **Workflow caveat:** the auto-abbreviate-uncheck, cache-clear, and primary-source Prefix/Suppress (Classic Add Citation) tricks are Word/LibreOffice-plugin only. For Stan's Ctrl+Shift+C→Obsidian path, the dictionary/series/Annote fixes work at the data+style level regardless; journal auto-abbreviation may differ via Quick Copy (test). DSS/Philo/rabbinics → hand-type in footnotes (SBL puts primary-source sigla in notes, never bibliography).

Optional later: plugin's Semantic Search (needs embedding API key).

Related: [[rs6310-amridge-course]] (the Zotero work supports his RS 6310 sourcing + the Essential Elements paper bibliography), [[stan-user-profile]].
