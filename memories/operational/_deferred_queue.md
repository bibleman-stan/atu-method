---
name: deferred-queue
description: "The standing parked-work queue for the biblical-reader / ATU-method program — named arcs, v2/v3 refinement residuals, cross-corpus TODOs, research. Keep current; this is the answer to 'what's deferred'."
metadata: 
  node_type: memory
  type: project
  originSessionId: 87af68a0-0291-4910-962f-d0913b5722e6
---

> **PROVENANCE**: recovered 2026-08-06 from Claude Code file-history (newest 01f7ecc9f948752f@vN, state ~2026-06); possibly stale — re-verify before relying.


**The tracked queue of deferred/parked work.** Update as items start (move to in-flight) or land (delete). In-flight work is NOT here. Live deploy state = `atu-method/2-evidence/deployment-status.md`.

## Pointer-integrity findings — first run of the retargeted checker (2026-08-06)
`5-machinery/scripts/check_broken_pointers.py` retargeted from its reader-repo layout to this hub + taught to validate link anchors. First run: 86 files, **0 broken anchors**, **57 broken doc paths**, 80 advisory non-`.md` (reader-repo scripts, not failures). Not fixed this turn — several sit inside Stan's in-flight `canon-index.md`. Ranked:
- **`change-protocol.md` (7 citers) — the real one.** `00-start-here.md:53`, `1-method/glossary.md:13`, `4-process/retraction-log-protocol.md:56` + 4 more cite it, but the only copy is `_old/change-protocol.md`; §7 change-discipline was extracted into `framework.md` §7.0–§7.9 by commit 93d67f5. Citers need repointing to `framework.md §7.x`.
- **`structural-licenses.md` (15 citers, all in `canon-index.md`)** — known-not-yet-existing per that file's own hard-constraint #14; resolves when the §-renumber migration lands, not before.
- **`feedback_taskmaster_propagation.md` (2 citers)** — cited by `memories/feedback_compaction_is_session_boundary.md:8` and `memories/feedback_dont_over_engineer_orchestration.md:8`; exists nowhere. Possibly a casualty of the namespace deletion that the recovered `MEMORY.md` index never listed — check file-history before assuming it was renamed.
- **`R17-poc-operational.md` (4)**, **`retraction-log.md` (3, reader-repo)** — lower priority.

## Docs interlinking — remainder (2026-08-06)
`5-machinery/scripts/link_canon_refs.py` converted plain-text canon cross-references into clickable markdown links across the 9 git-clean docs (verified: unwrapping every link reproduces HEAD byte-for-byte; every destination resolves). Remaining:
- **4 docs skipped because they carry uncommitted in-flight prose** — `00-start-here.md`, `1-method/glossary.md`, `3-implementation/substrate.md` (modified), `1-method/cross-corpus-principles.md` (untracked; the largest win at 53 lines). Run `python 5-machinery/scripts/link_canon_refs.py --apply --files _index.md glossary.md substrate.md cross-corpus-principles.md` once that work is committed, so link edits don't entangle with Stan's prose.
- **2 DEAD section-IDs surfaced, deliberately NOT repointed** (silent repointing is the failure the canon-xref arc exists to fix): `glossary.md:11` cites `framework.md §1.2` and `glossary.md:54` cites `framework.md §1.3` — live framework §1 is "Purpose" with no sub-sections. Same phantom class [[canon-index]] tracks; needs a Stan ruling on the correct live successor.
- **`memories/**` not linked** — that layer uses `[[wikilink]]` convention, not markdown links; deliberate scope split, revisit only if the two conventions cause friction in the vault.
- **`5-machinery/scripts/check_broken_pointers.py` is retargeting-stale** — recovered 2026-08-06 but written for a reader-repo layout (`REPO_ROOT=parents[2]`, scans `private/01-method/`, `handoffs/`); it does not scan this hub's `docs/` at all. Retarget + add anchor-resolution checking (every `](<file.md#heading>)` destination matches a real heading) so link-rot is detected, not silent. Feeds the audit-tier mandate.

## Named arcs (big, explicitly deferred)
- **BHSA-canon-migration** (Tanakh, master-blaster Phase 1) — swap the word-FORM source TAHOT→BHSA (823-verse / 3.5% mismatch). ATU breaks already shipped; this is forms only. JSONL pointer in [[_named_arcs]].
- **Binding-engine extraction** (Phase 7) — pull the shared engine into `atu-method/atu_method/bindings/`.
- **BoFM Firestore-PWA purge** (Phase 6X) — dead-code experiment cleanup.

## Discipline-gate bypass hardenings (5 remaining from bypass-audit w0e8ald2d verify-1, 2026-06-05)
The 2 CRITICAL bypasses (#5 execute-don't-ask carve-out, #6 Stan-verse-flag mechanical trigger / gate 11) shipped in commits 726fa3a + 89e2209. The 5 remaining (HIGH + MEDIUM) are NOT yet hardened:

- **Bypass #1 (HIGH) — Gate 10 regex citation list is finite allowlist.** Misses `framework §2.3` (any §-number outside `[12]`), `§v1.6` (workspace prose mentions but gate-10 regex doesn't), `binding-rules-hebrew.md §B7` (binding-rules-*.md not enumerated in regex), `apparatus.md` / `substrate.md` / `methodology-position.md` / `toolset-architecture.md`. **Fix:** replace allowlist with pattern classes — `(framework|binding-rules-[a-z]*|apparatus|substrate|toolset-architecture|methodology-position)\.md` and `§(\d+\.\d+|v\d+\.\d+|[A-Z]\d+)`. Either alone triggers the gate.
- **Bypass #3 (HIGH) — Gate 10 file-edit shape regex enumerates `Edit.*bofm_v1_fabric\.py`** misses `Write` (different param name `content` not `new_string`), misses new helper module files (`_amplificative_marker.py`, `bofm_v1_fabric_amplificative.py`), misses one-line import-statement edits to the fabric. **Fix:** replace with `(Edit|Write).*\b(_fabric|generate|binding|atu|overrides|merges)\b`, and scan FULL Write `content` parameter not just `new_string`.
- **Bypass #4 (HIGH) — Paraphrase of firewall passes "verbatim" demand.** Gate 10 has no mechanical paraphrase-detector; a paraphrase that THREADS the firewall's key tokens inside paraphrase prose passes self-attestation. **Fix:** require the verbatim quote to be a Markdown block-quote (`> ` prefixed lines) AND the gate-10 evidence is "block-quote line substring-matches `framework.md` contents." Self-administered but falsifiable — Claude can be caught with a `> ` line that isn't in the file.
- **Bypass #7 (MEDIUM) — Gate 10 ordering ambiguity.** Prose says "rule does NOT get written to disk, NOT dispatched to a §7.3 audit, NOT shipped" but doesn't explicitly say Read precedes Edit. A Claude reading "I'm dispatching §7.3, which will verify canon-conformance" might tick the gate-10 box treating the audit AS verification. **Fix:** add explicit sequencing line to gate 10: "The Read + verbatim quote must precede the Edit/Write tool call (not the audit). §7.3 is a downstream safety net, not the citation-verification mechanism."
- **Bypass #8 (MEDIUM) — Commit-message citations dodge gate 10's draft scan.** Standing default #6 lists "comment, docstring, or commit message" as canon-citation triggers; gate 10 scans "the draft (and any pending Edit/Write `new_string`)." Commit messages go through Bash `git commit -m` via HEREDOC, not Edit/Write `new_string`. **Fix:** extend gate 10 scan target to "the draft response, any pending Edit `new_string`, any pending Write `content`, and any pending Bash command containing `git commit`."

Action when next touching the discipline: enumerate, batch-apply, commit. Source of these findings: `C:\Users\bibleman\AppData\Local\Temp\claude\C--Users-bibleman\455e2f1f-bcaa-412e-bc18-411016572c77\tasks\w0e8ald2d.output` verifyResults[0] (bypass-audit lens).

## Cross-verse forward-closure (ACTIVE — being built this turn, 2026-06-05)
- **Cross-verse bare-`that`-complement class** (BoFM): 18 verses corpus-wide where verse N opens `[DISC]? (even )? that` AND verse (N-1) ends in open valency (--, ,, ;, :). Canonical: Alma 34:4 — opens "Yea, even that ye would have so much faith..." as bare complement of 34:3's `exhorted`. Stan raised mid-session 2026-05; diagnosed as "issue #5: post-emit forward-closure backstop"; SILENTLY PARKED (see [[feedback_no_silent_parking]]). Full enumeration: 1Ne 5:18, 10:3, 21:9; 2Ne 1:22, 9:2, 15:19, 24:25; Mosiah 27:4, 28:2; Alma 16:17, 34:4, 34:38, 39:13; Helaman 16:18; 3Ne 12:45, 13:4, 13:18, 15:17. Fix shape: at v2-generation time, detect the pattern via parse `mark`-deprel "that" at verse-initial position (or DISC+that) AND prior verse's last-line trailing-token signature; render verse N's first line as continuation of verse (N-1)'s last line (architectural choice between text-merge vs continuation-flag pending; see in-progress design).

- **Cross-verse AICTP-frame class** (BoFM, surfaced 2026-06-06 during Enos 1:1 review). Pattern: verse N opens `(Behold,)? it came to pass that I/he/they, NAME, [PARTICIPIAL-FRAME]…` whose finite-VP closure does NOT land within verse N; verse (N+1) supplies the matrix finite verb under "And I/he/they + finite-VP". The AICTP frame's `that`-complement is forward-incomplete throughout verse N (subject established as nominative-absolute, never resolves to its finite predicate). Enos 1:1+1:2 is the anchor: v1 establishes "I, Enos, knowing my father…" as nominative-absolute; v2's "And I will tell you of the wrestle…" supplies the finite VP for `I`. Per `framework.md §2.1` BOTH lines must independently satisfy forward-closure for a break to be licensed; v1 alone fails → §2.1 forbids the verse boundary, v1+v2 = one ATU. **Corpus-wide enumeration TODO**: scan v0 for verses opening `(Behold,?)?\s*(And\s+)?it came to pass that\s+(I|he|they|we),\s+\w+,\s+\w+ing` whose verse-internal parse never closes finite-VP on the matrix subject; cross-check with verse (N+1) opening `And\s+(I|he|they|we)\s+(will|did|do|shall|have|had)\b`. Fix shape: same architectural lever as the bare-`that` class — render verse N as continuation of … verse (N+1) (NOTE direction reversed from bare-`that` class: AICTP frame continues *into* the next verse).

## tanakh-morph — deferred final-audit residue (2026-06-12, wf_42e53d9b-bd5; HIGH/CRITICAL all shipped in 771b109)
Remaining lower-priority audit items, ranked by the gaps-lens (value/cost), to do BEFORE or ALONGSIDE the ROOT layer:
- **Colour-semantics alignment (MEDIUM):** verb-form FILLS contradict Stan's own xlsx colour language — his infinitive=purple #7030A0, participle=deep-blue #004F88. The reader's inf fill is grey, ptc fill is purple. Re-map so ptc/inf fills honour his hexes (lifted for dark-bg contrast: inf #b07fe0, ptc #5b9fd4) — but this collides with the imperfect BLUE fill, so it's a real decision (move imperfect off blue, or accept). Ship before ROOT so the ROOT layer's pattern-vowel translit colouring lands on correct inf/ptc hues.
- **Inf-constr vs inf-abs + ptc-active vs ptc-passive split (MEDIUM):** the vf-inf and vf-ptc toggles each merge two forms BHSA distinguishes (vt infc/infa, ptca/ptcp). One more granularity split (same pattern as the wayyiqtol/perfect/... split already shipped) to reach his full taxonomy. Data already in JSON (t-infc/t-infa/t-ptca/t-ptcp classes exist).
- **Lexicon/concordance deep-link (LOW):** tap panel is a dead end; add a link out (e.g. to a lexicon by lemh, or a "show all occurrences" using lem-equality — overlaps the ROOT-layer root-echo).
- **Pseudo-element channel map (do BEFORE ROOT):** ::after is now freed (Aramaic marker converted to a real span in 771b109); ::before is still binyan-owned (hif/hof/hsht third line). The ROOT layer's R2 pad + radical bolding need real child spans or a documented channel map, or collisions multiply. Converting the construct dash + binyan third-line to render-time spans is the durable shape.
- **Cosmetic residue (LOW):** after 4 rounds of template surgery — audit flagged dead toggles, unused const, unstyled classes, stale panel tints, legend/var label drift. One zero-risk cleanup pass.

## tanakh-morph KNOWN-WORDS / vocab-acquisition tracker (NEW feature, 2026-06-12 audit — highest-value AFTER root layer)
The mem-column half of Stan's BH-vocab.xlsx model, absent from the reader. Per-user "I know this lemma" state (localStorage by lemh/lex), feeding the gloss frequency-band system: instead of (or alongside) freq bands, gloss only UNKNOWN words. ~1-2 days. Extends territory no competitor (Logos/STEP/Parabible) holds. Suffix-aware (track lemma, not surface). Could ingest his actual xlsx mem/chapter columns as the seed known-set, or his GGBH textbook-chapter progression as a gating source. Gaps-lens ranked this #5 overall, #1 of the NEW features after ROOT.

## tanakh-morph ROOT layer — SHIPPED 2026-06-13 (commit 9bc84d3)
Built per the design below. `src/root_match.py` aligns ETCBC root → stem skeleton, fail-closed; 97.7% of 73,710 verb tokens get in-text radical marking, 1,720 doubly-weak fall back to panel-only. Root dropdown (Radical skeleton + R2 spotlight), panel Root row (root + weak-class chips + loss exp), root-echo on tap. NOT yet built from the original design: (f) translit pattern-vowel colouring for inf/ptc (inf #b07fe0, ptc #5b9fd4 — Stan's lifted hexes); root layer is VERBS ONLY (noun root display deferred — segholates etc. messier). lqx exception set is just {'LQX['} — extend if other assimilating-ל roots surface. Original design (superseded by the build, kept for the deferred pieces):

## tanakh-morph ROOT layer — Phase 2 of refinement-2 (designed 2026-06-12, NOT yet built)
The root-and-pattern centerpiece from Stan's BH-vocab.xlsx taxonomy. Full design exists in workflow wf_1ed7ae64-961 output (root-pattern lens, with verification receipts from real build JSONs). Headlines: (a) Shoresh toggle — radicals bold-white inside the stm segment, matres dimmed .55, R2 copper pad (his Excel R2-highlight habit ported to glyph level); (b) regen-time DP subsequence aligner lex-consonants→stem-graphemes with weak-root equivalence sets (R1=י↔ו, hollow R2 absent, III-ה apocope), classified losses (assim/elided/hollow/apocopated, e.g. yitten dagesh = assimilated nun), FAIL-CLOSED (no marking below confidence), ~15-entry exception table (לקח, הלך), per-book validator tiers; (c) baked JSON field `rt:{h,cls,map,loss,exp,conf}`; (d) root-echo interaction (tap → ring all same-lem words in chapter); (e) weak-class chips in tap panel (I-Nun, III-He...); (f) translit pattern-vowel coloring for inf/ptc using Stan's lifted hexes (inf #b07fe0 ← his #7030A0, ptc #5b9fd4 ← his #004F88) — his vowel-pattern recognition cues (shewa-o/a-o/a-u/o-e); (g) prs person/gender/number added to prs segs (BHSA prs_ps/prs_gn/prs_nu — verified absent from current JSON); (h) Root ▾ becomes top-level toolbar dropdown next to Binyan (root+pattern = his mental model pair); toggle ids reserved: root/r2/weak. Toolbar slot already freed by the Grammar consolidation. Anchor test fixtures: יִּתֵּן NTN, יָּקָם QWM, יִּבֶן BNH, יֵּשֶׁב JCB, תֵלֵךְ HLK, יִּקַּח LQX, מְרַחֶפֶת piel ptc.

## Reader UI — cross-verse-merged empty-verse gap (Stan-flagged 2026-06-06)
When a verse N is cross-verse-merged into verse (N-1) (e.g. Alma 34:3+34:4, Enos 1:1+1:2), the current HTML rendering shows: (a) verse (N-1)'s content with a superscript `N` marker at the merge point (correct); (b) an UGLY GAP below where verse N's standalone marker (`34:4` / `1:2`) appears with no content. Stan's call: **hide the empty verse marker entirely** (preferred). Fallback: if we keep the standalone marker, then hide the superscript `N` indicator inside verse (N-1) to avoid double-marking. **Decision toggle**: also handle the user-level "hide verse numbers" reader setting — when that's on, the superscript `N` indicator inside the merged content should also be hidden. Fix lives in `readers-bofm/scripts/build_book.py` (or template/CSS). Applies wherever cross-verse merge fires: existing Alma 34:3+4 + the 17 other bare-`that` class verses (1Ne 5:18, 10:3, 21:9; 2Ne 1:22, 9:2, 15:19, 24:25; Mosiah 27:4, 28:2; Alma 16:17, 34:4, 34:38, 39:13; Helaman 16:18; 3Ne 12:45, 13:4, 13:18, 15:17) AND the forthcoming AICTP-frame class (Enos 1:1+1:2 anchor; corpus-wide enumeration pending — see above).

## Substrate-enrichment audit-pipeline defects (surfaced 2026-06-06 during Enos 1:1 review)
The Enos testbed workflow (w6zy5k3e0, 2026-06-06) used per-sentence LLM agents to surface candidate ATU-break positions. The first Stan-review verse (Enos 1:1+1:2) exposed three systematic defects in the audit prompt and pipeline shape. NOT addressed in this turn — fix BEFORE corpus-wide rollout.

- **Defect 1: §2.1 forward-closure tested on only the SECOND piece of split candidates.** Audit agents (and Claude's initial analysis) tested "does B alone forward-close" for a candidate split A|B, but never tested "does A alone forward-close." Per `framework.md §2.1` line 37: "A break between two adjacent lines is licensed if and only if BOTH lines independently satisfy these two conditions." The audit tested half the rule. Enos 1:1 candidate 1 ("for") and candidate 2 ("and blessed be") both have A-piece forward-closure failure (subject `I` of v1 never gets a finite VP within v1) that the audit missed. Fix: rewrite the audit-agent prompt to require explicit two-piece forward-closure test with verbatim §2.1-clause quotation.
- **Defect 2: Audit ran sentence-local; cross-sentence closure dependencies invisible.** Each agent analyzed one Stanza-sentence in isolation. Cross-sentence forward-closure (verse N opens incomplete; verse N+1 supplies closure — see the AICTP-frame class above) is structurally invisible to per-sentence audit. Fix: add a sentence-boundary pass that, for each candidate at sentence-end, checks whether the next sentence supplies needed closure (matrix finite VP for unresolved subject, complement for open valency).
- **Defect 3: AICTP-frame structural pattern needs framework recognition.** The verse-N AICTP-frame / verse-N+1 finite-VP resolution is a recurring BoFM pattern (analog to Hebrew vav-consecutive narrative-frame chains). Worth considering as named structural class in framework.md § (similar to "Serial circumstantial participial chains" / "Discrete cognitive-state circumstance chain" at framework.md:56-68). Stan-articulated 2026-06-06: "v1 feels like the entire AICTP front-end frame, and the vav equivalent resolution/completion does not come until v2."

Action when next touching the audit pipeline: address Defect 1 + 2 in the workflow script before re-spraying any book. Defect 3 is a framework-level proposal — surface during the canon-index halt resolution OR after, depending on Stan's scope preference.

## v2/v3 refinement (deploy-then-refine; no corpus has run a real v2-LLM/v3-editorial stage)
- **GNT residuals → v2/parse-repair**: `ὡς` (polysemous, unbound); intra-quote subordinate binds (Heb 1:13, Matt 5:26 stay split by quote-protection); long-period over-split tail; `_ENTREATY` reserved/unwired; performative "I say to you" (parataxis substrate).
- **BoFM named residuals** (see [[project_bofm_bidirectional_rebuild]]): subject-clause/`whoso` splits; em-dash artifact lines; appositive splits; bare-infinitive phrases; **poetry parallel cola over-merge (2Ne8 — needs MORE splits; port Tanakh NOUN-conj split)**; multi-proposition over-merge (wherefore-chains); periodic participial preambles; the subject-fracture read-and-fix override tail.
- **BoFM "take-a-look" pile (Stan-flagged, post-forward-frame):** (1) **Alma 32:13 — M1 causal-`for` gap**: `for a man … seeketh repentance` (own subject+verb) should split as its own grounds ATU (the EME γάρ-analog, = rule M1) but stays merged — nested `if`-protasis likely broke M1's own-subject test. (2) **Alma 32:19 — relative-clause mid-break**: `he that knoweth the will of God / and doeth it not than…` severs the relative's coordinate verbs; should be `he that knoweth … and doeth it not` (one ATU) vs the `than he that only believeth … falleth into transgression` comparison. Diagnose both after the forward-frame ship lands (can't regen mid-edit).
- **BoFM lexicon gap** — ~7 EME thou-verbs missing from the modern-mode swap lexicon (mayest/mightest/beholdest/beheldest/knewest/deniest/commandest). Stan's call (display change + HTML rebuild). `archaic_normalize.gaps()` reports them.

## GNT ὅτι via Macula (ACTIVE next step — NOT v2-deferred)
- **Re-source the GNT ὅτι bind/stand decision from Macula Greek**, not lemma lists. Macula tags `rule=that-VP`(complement→bind) vs `sub-CL`/`role=adv`(causal→stand), + `person`/`subjref`/`referent` for the direct-vs-indirect deixis split. The 2026-05-26 lemma-list attempt produced 6 regressions (causal over-binds Matt 5:36/John 2:18/1Cor 3:13 + side-effects John 16:21/Acts 14:9); prototype off-engine, validate against the 41-verse change set. Ship the regression-free `_junction_same_verse` guard separately. See [[_open_checkpoint_2026-05-26]] + [[reference_greek_datasets]] + [[feedback_mechanical_first_for_own_review]].
- **Lit-mined rule candidates to test mechanically** (2026-05-26, Korpel/Marschall/Scheppers): (1) **γάρ-boundary** (Marschall T9: γάρ ≈ always begins a period/autonomous côlon → split-before, Macula-checkable); (2) **don't bind on conjunction/participle token alone** — condition on Macula clause `role`/`rule` (corroborates the re-sourcing); (3) **full-GU condition** (Scheppers/Croft: ATU = grammatical unit with its full obligatory-complement set → forward-closure formalization over dependency arcs); (4) **parallelism constraint** (coordinate structures can't share a colon; exempt short lexicalized pairs); (5) **argument-vs-adjunct distance gradient** (bind arguments tight, let adjunct PPs float); (6) **RC split rule** (Croft: final/extraposed RC → own ATU; clause-internal restrictive RC → never split from head); (7) **ellipsis→bind** (Korpel: a verb-gapped colon fails backward self-containment → binds — port to Hebrew B-catalog); (8) **emphasis closed list** as boundary-promotion cues (Korpel: fronted S/O/adjunct, vocative, deictic, direct-speech marker, imperative/jussive, interrogative). CAUTION (Scheppers): do NOT adopt his fronting-makes-a-colon / sub-clausal granularity — that IS our documented over-split failure mode.
- **Validation oracles to build** (independent boundary checks): Korpel's ~92% Masoretic-disjunctive-accent agreement (Tanakh ATU-vs-te'amim measurement — corroboration, not a force); Marschall's hand-segmented **2 Cor 10–13** as a GNT gold set; Scheppers' P1/P2 Wackernagel postpositive position as a Greek-only boundary validator.
- **binding-rules-greek.md** — the Greek catalog (analog of `binding-rules-hebrew.md`), never written; home for per-rule Macula feature-signatures (`rule`/`role`/`person`/`referent`) + the lit-mined rules above. Write after the Macula re-sourcing validates.

## Substrate-gated fabric rules (PARKED 2026-06-03 — §7.3 needs-revision on both, blocked on substrate gaps)

- **GNT μέν...δέ correlative-split fabric rule (Lever-1) — PARKED.** Both text-layer AND clause-layer designs failed §7.3. Text-layer over-splits ~20+ distributive-nominal FPs (1Cor 1:23, Eph 4:11, Mark 12:5, Matt 16:14 class — shared elided matrix). Clause-layer with `_has_finite` UNDER-splits its own headline targets (Rom 8:10, Matt 26:41 are verbless equational — `_has_finite(A)∧_has_finite(B)`=False∧False → no split). The εἴτε/οὔτε analogy doesn't transfer. **Required substrate gaps before reopening**: (a) `_has_matrix_finite(ws)` scoped to matrix depth (excludes finite verbs inside relative clauses, ὅτι/ὅπως/ἵνα-headed sub-spans, direct-speech quote spans); (b) `_is_equational_cola(ws)` detecting overt nominative subject + adjacent predicate nominal/adjective; (c) shared-matrix-antecedent detector via v0/lowfat clause-atom governor lookup that BLOCKS the split when both cola share a governor outside the segment. Interim: route Rom 8:10 / Matt 26:41 through Lever-3 overrides if needed. Truth-set "61 already split" count predates redesign — needs fresh per-verse provenance accounting (lowfat-segmentation vs `_dev_connective` vs verbless-merge-skip).

- **Tanakh B7 wəqatal-pair extension fabric rule (Lever-1) — PARKED.** §7.3 verdict: needs-revision on FOUR independent blocking findings. (1) **Substrate gap**: `prev_is_bare`/`curr_is_bare` predicates don't exist; clause dict in `run_full_tanakh.py:292-304` carries no `n_overt_nominals` / sp-aggregate / `nsubj` flag. (2) **Standing-rule conflict**: `feedback_rhetoric_figures_constrain_atu` (codified at `validators/_shared/hendiadys_lemma_pairs.py:9-20`) explicitly forbids hendiadys as a mechanical determiner — "would replicate the BoFM precedent failure." The proposed B7 extension is justified explicitly on hendiadys grounds. (3) **Class heterogeneity**: 77-member class conflates Deuteronomic exhortation (hendiadys, merge) + prophetic threat (Jer 20:6, distinct sequential events, split) + Daniel apocalyptic (Dan 7:24, split) under identical bare-WQt+bare-WQt substrate signature — opposite true-dispositions. (4) **No subject-agreement gate**: Isa/Jer chains routinely shift subjects (YHWH 1cs ↔ Israel 3cp); without ps/nu/gn check, would over-merge ~28% of class (Stan's red line). Chain-arity defect: binary rule + linear driver produces 3-way transitive merge on Deut 7:13 — would collapse three theologically distinct divine acts. **Required substrate gaps before reopening**: (a) add `n_overt_nominals` / `is_bare:bool` flag in `extract_clauses_for_chapter` at run_full_tanakh.py:286-290 by extending word-node loop to count `pdp` categories outside {verb, conj}; (b) verify whether BHSA's WQt0 typ-zero suffix already encodes bareness (the new flag may be redundant); (c) add subject-agreement gate (`ps`/`nu`/`gn` features from BHSA verbal morphology); (d) keep WAYYIQTOL_TYPES and WEQATAL_TYPES as disjoint sets with same-set gating (no unified NARRATIVE_FINITE_TYPES); (e) chain-length guard (max=2 OR different head-verb-lemma domain on 3rd member); (f) re-justify on non-hendiadys grounds OR formally revise the `feedback_rhetoric_figures_constrain_atu` standing decision in writing with BoFM-precedent-failure risk acknowledged. Interim: route Deut 6:11 through Lever-3 overrides if needed. **2026-06-03 substrate pipeline launched to build (a)+(b)+(c) for Tanakh and (a)+(b)+(c) for GNT, audit-gated.**

## Cross-corpus / atu-method
- **Greek + EME-English + Latin binding-rule catalogs** — TODO in `atu-method/00-start-here.md` (GNT R-rules exist in-repo but aren't formalized cross-corpus).
- **Dir-name normalization** — Tanakh `v2/heb`, BoFM `v2` → the framework v1.5 stage name (see [[corpus-pipeline-layer-map]]).
- **LXX / Vulgate v1-substitutes** (own treebank) — parked behind BoFM ([[project_corpus_v1_substitutes]]).
- **Aramaic portions of the Tanakh** (Stan-flagged 2026-06-02; DE-RISKED + VERIFIED EMPIRICALLY 2026-06-03 via BHSA TF query) — Daniel 2:4-7:28 + Ezra 4:8 + 5:1-6:18 + 7:12-26 are currently HELD (no ATU output; v0 prose only). **EMPIRICAL FINDING (2026-06-03)**: BHSA contains **1,375 Aramaic clause_atoms with full Hebrew-parity schema** (Dan 1,070 + Ezra-1 241 + Ezra-2 64), 3,130+778+181 phrase_atoms decomposed per clause, all `typ`/`function`/`rela` features populated identically to Hebrew. **CRITICAL CAVEAT**: BHSA does NOT tag `language=arc` at the clause_atom level (only at *word* level returns `None` for clause_atoms). The Aramaic ranges must be **hardcoded as verse-range guards** in any port — `is_aramaic_verse()` in `tanakh_overrides.py` already has this pattern. **Port readiness (per 2026-06-03 recon agent)**: 9 of 13 binding rules (B1, B2, B6, B7, B8, B9, B10, B12, B14) are **easy-port — purely structural**. 3 are hard: B3 `אשר`→`די` (relative-marker swap), B5 temporal anchors (audit Aramaic anchor inventory), B11 cognition + ki-complement (audit Aramaic cognition lemma set). **Recommended first port**: B1 (vocative) or B6 (casus pendens) — zero Aramaic adaptation cost, proves the pipeline end-to-end. All other treebank options surveyed are non-starters (no UD/PROIEL Aramaic, Macula-Hebrew doesn't cover Aramaic, CAL is lexical not syntactic, no Targum to Dan/Ezra, Syriac is dialect-wrong + morph-only). Override harness `tanakh_overrides.py::is_aramaic_verse()` explicitly skips these ranges so a stray override can't silently ship in the interim.

## Research / tooling
- **FEF/AICTP paper** (JBMS) — offered-but-unbuilt one-page parallel-examples evidence sheet (B5 / AICTP / FEF). [[project_fef_aictp_paper]].
- **Zotero MCP** — pending read/write test. [[reference_zotero_mcp]].

## Stale-memory housekeeping (flagged, mostly low-priority)
- `project_bom_reader` (66d stale, git-workflow + already-fixed sacred line), `project_readers_nt` (45d stale: repo is readers-gnt, mechanical-first), `user_stan` (git-workflow section outdated). Index notes flag these. Fix opportunistically or retire.
