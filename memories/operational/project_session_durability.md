---
name: project-session-durability
description: "JSONL transcripts were silently hard-deleted by Claude Code's 30-day `cleanupPeriodDays` default; FIXED 2026-06-01 (set to 36500). Tax/house convos un"
metadata:
  node_type: memory
  type: project
---

> **LOST FILE — STUB ONLY (2026-08-06).** The full original was deleted with the `~/.claude/projects/C--Users-bibleman/memory/` namespace and was NOT recoverable from file-history, jsonl-archive Writes/Edits/Reads, or Dropbox backups. Everything below is the surviving index one-liner from the recovered `MEMORY.md` (@v44, 2026-06-15) — an inference-bearing summary, NOT the original record. Do not cite this as the warrant file.

JSONL transcripts were silently hard-deleted by Claude Code's 30-day `cleanupPeriodDays` default; FIXED 2026-06-01 (set to 36500). Tax/house convos unrecoverable. Remaining gaps: transcripts unbacked (git excludes `projects/**`), only biblical memory namespace version-controlled. Federation = per-launch-folder memory namespace; cross-history index at `~/.claude/session-index.json`.
